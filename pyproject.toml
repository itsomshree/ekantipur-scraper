[project]
name = "ekantipur-scraper"
version = "0.1.0"
description = "Playwright-based scraper for ekantipur.com — extracts entertainment news and cartoon of the day"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.59.0",
]
